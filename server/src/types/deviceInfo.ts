//Same type in the client ( see client/utilities/deviceInfo), check this out later to remove duplicate code

export type DeviceInfo = { name?: string; userAgent?: string; timeZone?: string,mobile?:boolean };